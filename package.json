{
  "name": "leaderboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "meteor --settings settings-development.json",
    "prod": "meteor --settings settings-production.json"
  }
}
